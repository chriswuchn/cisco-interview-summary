1、真题给定一个英文字符串,请写一段代码找出这个字符串中首先出现三次的那个英文字符(需要区分大小写)。
python解法：
if __name__=='__main__':
    keydict={}
    string=input()
    for s in string:  
        if s.isalpha() or s.isdigit():
            if s in set(keydict):
                keydict[s]+=1
                if(keydict[s]>=3):
                    print(s)                   
                    break
            else:
                keydict[s]=1
  
            
C++解法
#include <stdio.h>
#include <string>
#include <iostream>
using namespace std;
int main()
{
	string s;
	//char s[100];
	//gets(s);
	getline(cin,str);
	//note:cin>>s有问题，碰到NULL会停下来	
	while(cin>>s){}不会跳出来，只有按了enter+Ctrl+z（windows）+enter才会跳出来；这样可以储蓄输入和计算
	int arraysum[256] = {0};
	for (int i = 0; i < s.length(); ++i)
	{
		if( ++arraysum[str[i]] == 3){
            		if(str[i] >= 'a' && str[i] <= 'z' || str[i] >= 'A' && str[i] <= 'Z'){
               			cout<<str[i]<<endl;
                		break;
			}
            }
        }
	return 0;
}
